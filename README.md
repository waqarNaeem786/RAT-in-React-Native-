# Testing the React Native code Base

To start the application:
```sh
	npx react-native android-run
```

To refresh the dependences:

```sh
	sudo npm install --save-dev @react-native-community/cli
```
